# .gemini/commands/review.toml
# Where: generated Gemini CLI command adapter.
# What: Gemini command adapter for .agents/commands/review.md.
# Why: Gemini CLI discovers custom commands as TOML files.
# Generated by tools/sync_agent_assets.py. Edit the shared source instead.

description = "Shared repository command: review"
prompt = """
# Review Current Work

Review the current worktree or selected files with a code-review stance.

- Lead with concrete findings ordered by severity.
- Focus on bugs, behavioral regressions, missing tests, secret leakage, and unsafe workflow changes.
- Ground every finding in file and line references when possible.
- Keep summaries secondary to findings.
- If no issues are found, say that directly and name any remaining test gaps.

User arguments from Gemini CLI, if any:
{{args}}
"""
